# MaaBanGDream vX.Y.Z

> 发布说明模板：每个 Release 的简介用下面结构，主页“更新公告”会原样渲染这份
> Markdown（含 emoji）。`## 验证` 一节必须如实区分“自动化验证”和“真机验收”。

## ✨ 新功能

- 🆕 功能点一：一句话说明解决了什么问题、对用户意味着什么。
- 🆕 功能点二。

## 🐛 修复

- 🛠️ 修复点一：现象 → 根因 → 修复（一句话链路）。
- 🛠️ 修复点二。

## 📦 更新方式

- 🔄 启动客户端会自动检查更新并按设置自动下载；更新后安装目录名自动变为
  `MaaBanGDream-vX.Y.Z-win-x64`。
- 📢 更新后主页会展示本次发布公告（本页内容）。

## ✅ 验证

- 🧪 自动化：`scripts/verify.ps1` NNNN passed / N skipped；发布包结构校验通过。
- 🎮 真机（雷电 / MuMu）：写清测了什么、几局、结论；未验收的必须写明“待真机验收”。

## 🔒 已知限制

- ⚠️ 未解决或暂缓的问题（如有）；没有就删掉本节。
