# MaaBanGDream v1.3.8

## ✨ 组曲演奏

- 🎼 新增“组曲演奏”：支持自由巡演与课题巡演，一首歌计一次，输入 3–99 且必须为 3 的倍数。
- 🎵 自由巡演支持“当前曲目”和“每首随机”，歌曲可以重复；歌曲身份延后到各曲准备页确认，准备页证据不足时由最终封面与实际标题在触控前补全。
- 💾 新增原子组曲会话。第 2/3 曲准备页和延迟结算可在会话、阶段、选项及歌曲身份一致时继续，用户停止保持中性，不会破坏旧 Profile 或校准会话。
- 🎯 每曲使用独立 run ID，并在第三曲后依次读取三张 PGGBM，分别保存成绩及回写正式演奏 timing offset。

## 🐛 结算与恢复

- 🪟 识别第一、二曲后可能出现的“达成报酬一览”弹窗，并仅用 Android BACK 关闭后重新判断准备页。
- 📊 识别第三曲后的组曲分数汇总页，等待页面真实离开后再读取 PGGBM，避免连续 BACK 跳过第三张结果或在主页触发退出确认。
- 🔁 已知结算页使用 Android BACK 与可见右下角按钮推进；持续未知页面交替尝试两种输入，每次重新识别。
- 🛟 未知结算页达到有界次数仍无法恢复时保存现场并调用 `CommonRecover`，必要时重启一次游戏回主页；恢复主页不会把未完成组曲误报为成功。
- ✅ 修复次数选项只覆盖总数、导致失败分支被错误报告为成功的问题。

## 📦 更新方式

- 🔄 v1.3.7 可通过 MFA 原生更新入口检查并下载 v1.3.8；已有 Python runtime 时优先使用较小的 `-update.zip`。
- 📁 默认版本目录更新后改名为 `MaaBanGDream-v1.3.8-win-x64`；自定义目录名保持不变。
- 🎼 本地谱面继续通过“演出设置 → 谱面辅助 → 同步”独立维护，不进入常规更新包。

## ✅ 验证

- 🧪 主项目完整自动化与固定运行时兼容检查通过；完整包和 runtime-free 更新包均执行结构、UTF-8 文件名、隐私路径和 SHA-256 校验。
- 🎮 雷电 Native 已完成一组自由巡演“当前曲目”真机闭环：三首均完成、三张 PGGBM 均保存并回到主页，任务以 3/3 成功结束。
- 🎮 另一次课题巡演续跑已完成剩余第 2/3 曲；本版新增的分数汇总页识别和通用未知页面兜底基于当前实拍模板完成自动化与开发部署验证，尚未再次完整跑完最终结算。

## 🔒 已知限制

- ⚠️ 自由巡演随机歌曲、课题巡演完整新开局、多组计数及不同中断阶段仍需分别积累真机验收。
- ⚠️ MuMu Native 仍受逐局变化的客户机时钟偏斜影响，当前建议雷电使用 Native、MuMu 使用 Legacy。
